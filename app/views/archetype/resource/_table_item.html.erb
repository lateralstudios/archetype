<tr>
  <td><%= check_box_tag :selection %></td>
  <% resourceful.attributes(:index).first(5).each do |attribute| %>
    <td><%= attribute.from(table_item) %></td>
  <% end %>
  <td><%= render 'table_item_controls' %></td>
</tr>
